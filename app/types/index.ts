export interface CustomButtonProps {
    text: string;
    background: string;
    height: number;
    width: number;
}

export interface SidebarTabProps {
    tab: string;
    values: string[];
}

export interface CuisineProps {
    img: string;
    title: string
}